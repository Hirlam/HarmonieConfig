{
  "type" : "object",
  "description" : "These variables need to be put in a category or removed (moved to the scripts). Click properties to see if there are more variables.",
  "required" : ["test","RUNNING_MODE", "SIMULATION_TYPE", "SINGLEOBS", "USE_MSG", "MSG_PATH", "Config", "hostdescriptions"],
  "properties" : {
     "RUNNING_MODE" : {
      "type" : "string",
      "description" : "Operational implies that Not STAGE is done for MARS requests and No assimilation is done if no obserations are found by Bator",
      "enum" : [
        "research", 
        "operational"
      ],
      "default" : "research"
    },
    "SIMULATION_TYPE" : { 
      "type" : "string",
      "enum" : [ 
        "nwp",
        "climate"
      ],
      "default" : "nwp" 
    },
    "SINGLEOBS": {
      "type": "string",
      "enum": [
        "yes",
        "no"
      ],
      "default": "no"
    },
    "USE_MSG": {
      "type": "string",
      "enum": [
        "yes",
        "no"
      ],
      "default": "no"
    },
    "MSG_PATH": {
      "type": "string",
      "default": "$SCRATCH/CLOUDS"
    },
    "Config" : {
      "$ref" : "config.schema.json"
    },
    "hostdescriptions" : {
      "$ref" : "hostdescriptions.schema.json"
    }, 
    "test" : {
       "title": "Video filename",
      "type": "string",
        "links": [
          {
             "href": "https://www.youtube.com/watch?v=OXbkpx-_fjM",
             "mediaType": "video/webm"
           }
        ] 
    }
    

  }
}
