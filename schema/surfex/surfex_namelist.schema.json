{

  "type" : "object",
  "title" : "Namelist",
  "description" : "Namelists are empty by default. Click properties to customize properties. >  to unfold the namelist. JSON to see the current values. Only NAM_TEB for now as an example.", 
  "options": {
    "disable_collapse": true,
    "disable_properties": true
  },
  "required" : ["NAM_TEB"], 
  "properties" : {
     "NAM_TEB" : {
       "$ref" : "nam_teb.schema.json" 
     }
  }


}
