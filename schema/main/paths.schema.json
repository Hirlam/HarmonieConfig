{
  "type" : "object",
  "title" : "Paths",
  "description" : "Location of observations, boundaries etc.",
  "options" : {
     "collapsed" : "true"
  },
  "required" : ["OBDIR"],
  "properties" : {
    "OBDIR": {
      "type": "string",
      "default": "$HMDATA/observations"
    }
  }

}
