{
  "type": "object",
  "description" : "Example of using the grid layout style. Current on/off style based on include.ass. Better to use booleans in which can we can use select box. Alternative is to use a single array with multi-select",
  "properties": {
    "SYNOP_OBS": {
      "type": "integer",
      "enum": [
        0,
        1
      ],
      "default": 1
    },
    "MODESEHS_OBS": {
      "type": "integer",
      "descriptin": "Mode-S EHS",
      "enum": [
        0,
        1
      ],
      "default": 0
    },
    "AIRCRAFT_OBS": {
      "type": "integer",
      "description": "AMDAR, AIREP, ACARS",
      "enum": [
        0,
        1
      ],
      "default": 1
    },
    "BOUY_OBS": {
      "type": "integer",
      "enum": [
        0,
        1
      ],
      "default": 1
    },
    "TEMP_OBS": {
      "type": "integer",
      "description": "TEMP, TEMPSHIP",
      "enum": [
        0,
        1
      ],
      "default": 1
    },
    "PILOT_OBS": {
      "type": "integer",
      "description": "Pilot, Europrofiler",
      "enum": [
        0,
        1
      ],
      "default": 1
    },
    "GNSS_OBS": {
      "type": "integer",
      "enum": [
        0,
        1
      ],
      "default": 0
    },
    "SGNSS_OBS": {
      "type": "integer",
      "enum": [
        0,
        1
      ],
      "default": 0
    },
    "LISTE_LOC": {
      "$ref": "liste_loc.schema.json"
    }
  },
  "format" : "grid"
}
